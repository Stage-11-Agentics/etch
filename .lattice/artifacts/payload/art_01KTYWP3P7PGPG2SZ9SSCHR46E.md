README restructured per operator feedback on ETCH-49: new 'What Etch does' section directly after the hero — 4-step mechanics (hooks fire invisibly → every session ends as a git object at refs/etch/sessions/<ULID>, crashes included → history queryable via query/git show → records travel over push/fetch to private remotes) plus a you-ask/you-run table making the wins concrete (overnight activity, file-touch provenance, per-ticket runs, crash detection, prompt audit). 'Why Etch' moved above the session excerpt, trimmed to pure differentiators (fleet/worktree-proof/sovereign/permanent; invisible+crash-honest folded into mechanics to kill duplication). 'What a captured session looks like' moved below the quickstart where it reads as step 4's result; transport prose trimmed, Agent Trace sentence kept. No new factual claims — all commands were binary-verified in the ETCH-49 pass; GFM rendering re-verified via GitHub markdown API (2 tables render, anchors intact, section order confirmed). PR https://github.com/Stage-11-Agentics/etch/pull/8 squash-merged (b4697b9), CI green, branches/worktree cleaned.