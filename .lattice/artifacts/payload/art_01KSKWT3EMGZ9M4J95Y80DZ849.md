Self-review passed. Acceptance criteria SPEC #9 met: agent-trace.json serialization in Agent Trace RFC v1.0 format. Types match OUTPUT_SPEC §4 exactly. Conversion covers: agent_id←Runtime, model←Model, session_id←SessionID, files←FilesTouched paths, timestamp←EndedAt with StartedAt fallback. 8 tests covering complete/incomplete sessions, empty/nil files, JSON round-trip, field names, single-entry invariant. Session types defined in schema package for ETCH-2 to consume. Removed one unused test helper (strPtr). No external deps added.