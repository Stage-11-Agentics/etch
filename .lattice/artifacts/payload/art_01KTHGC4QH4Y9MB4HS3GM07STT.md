Gates green on rebased branch (8222a83 atop origin/main 3dc67d4, post ETCH-17/20 merge): go test ./... PASS (incl. new TestFinalizeAgentSessionID + TestE2ESchemaPrivacyBatch), make build PASS, make smoke PASS. Manual temp-repo e2e with real binary: committed session.json shows agent_session_id='demo-upstream-A1' preserved verbatim while session_id is etch's minted ULID (01KTHGBKWN...); key present and null when upstream id absent (Finalize-level test — empty upstream id also disables end-hook correlation so it can't round-trip the binary). Crash-recovered records don't carry the field yet — rides with lifecycle worker's recovery-aggregator rework (per dispatch scope limits).