Hero second paragraph rewritten around the chosen value-prop pairing: flight recorder (what, retained as headline) × post-hoc forensics (why). New paragraph opens on the forensic moment — a change you don't recognize — and walks the recovery chain (session → prompt → ticket → other files touched → exit status), keeps the 30-sessions/4-worktrees/2-machines fleet color, and retains the next-agent-as-reader closer. GFM render verified via markdown API before push. PR https://github.com/Stage-11-Agentics/etch/pull/9 squash-merged, CI green, branches/worktree cleaned. Candidate sentences (5 what / 5 why) recorded in session transcript; alternates available if hero iterates again.