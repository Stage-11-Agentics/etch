Self-review passed. Implementation covers all acceptance criteria from SPEC #6. Three files added: internal/schema/session.go (cairn.session.v1 types with correct null-pointer JSON semantics), internal/recovery/recovery.go (ScanOrphaned, RecoverSession, CleanupWIP, RecoverAll, ReadTimeoutFromSettings + RefWriter interface stubbed for ETCH-7), internal/recovery/recovery_test.go (22 tests). Orphan detection uses dual heuristic: PID liveness check (primary) and timestamp-based timeout (fallback, configurable via .cairn/settings.json recovery_timeout_hours). Corrupt/empty .wip files are logged and skipped gracefully. All tests pass, go vet clean, no dead code.