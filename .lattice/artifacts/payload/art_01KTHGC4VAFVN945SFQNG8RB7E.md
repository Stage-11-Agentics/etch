Gates green on rebased branch (8222a83): go test ./... PASS, make build PASS, make smoke PASS. Manual temp-repo e2e with real binary, two repos, same machine: WITHIN-REPO STABLE yes (two sessions, identical hostname_hash), CROSS-REPO DIFFERENT yes (distinct salts -> distinct hashes). settings.json auto-generated with 64-hex hostname_salt; unknown-key preservation + atomicity covered by unit tests. .gitignore carve-out verified live: git check-ignore .etch/settings.json exits 1 (trackable), .etch/sessions/* still ignored. One-time commit-hint logged on salt creation. Cross-machine correlation within a repo requires committing settings.json (documented in README).