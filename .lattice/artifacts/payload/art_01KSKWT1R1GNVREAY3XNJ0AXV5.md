Self-review: PASS. All 22 tests pass across 2 packages (config: 5, redact: 17). Acceptance criteria SPEC #7 (hostname hashing SHA-256 default, raw opt-in via settings) and #8 (secret scanning/redaction before commit) are met. Pattern ordering is correct (Anthropic sk-ant- runs before OpenAI sk- to avoid false classification). Config reader handles missing file (defaults), valid JSON, partial JSON (merge), malformed JSON (error). Custom redaction patterns from settings are applied. No issues found.